# Issues
## General 
- The FAB button on `My Pets` tab and `Tasks` tab have different shadows, making them look slightly different and inconsistent.
- When building the project, the following warning is raised:
```
WARN  Found screens with the same name nested inside one another. Check:
Tasks, Tasks > Tasks
```

## `Tasks` Tab

## Add Task
When adding tasks from Today tab, using the FAB button, the task is a one-time only task. I want the users to be able to choose the type of the task they want.

## Date Display
The date display for the next 7 days tasks are in Gregorian format instead of Jalali format, as entered in the adding the task.

## Counter Display
The task counter in front of each section is in English format instead of persian (e.g. عقب افتاده . 0).

